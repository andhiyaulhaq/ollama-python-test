services:
  chat-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - OLLAMA_HOST=http://ollama:11434
    networks:
      - ai-network

networks:
  ai-network:
    external: true

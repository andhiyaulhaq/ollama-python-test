# Base URL for the backend API
# If hosted together via FastAPI StaticFiles or Vite proxy, you can leave this blank or exclude it.
# If hosting the frontend completely separately (e.g., Vercel), point this to your VPS IP:
# VITE_API_BASE_URL=http://<YOUR_VPS_IP>:8000
